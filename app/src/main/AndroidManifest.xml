<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.hostranger.zoomvideocallguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/customTheme">
        <activity android:name=".HowdoistartMeeting"></activity>
        <activity android:name=".WherecanifindDesktop" />
        <activity android:name=".AfterLaunchApp" />
        <activity android:name=".HowCaniLogin" />
        <activity android:name=".HowCaniStart" /> <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/app_id" />

        <activity
            android:name=".WhatisZoom"
            android:theme="@style/customTheme" />
        <activity
            android:name=".ItemActivity"
            android:theme="@style/customTheme" />
        <activity
            android:name=".GetStartedActivity"
            android:theme="@style/customTheme" />
        <activity android:name=".LogActivity" />
        <activity
            android:name=".Splashactivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>